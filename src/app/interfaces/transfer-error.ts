export enum TransferError {
  INVALID_ACCOUNT_TO = 'Invalid account to',
  INVALID_ACCOUNT_FROM = 'Invalid account from',
  INVALID_AMOUNT = 'Invalid amount',
  OVERDRAFT_PREVENTED = 'Overdraft prevented',
}
